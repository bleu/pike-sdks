{
  "name": "@pike-sdk/monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo run dev",
    "start": "turbo run start",
    "db": "turbo run db",
    "codegen": "turbo run codegen",
    "build": "turbo build",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "typecheck": "tsc",
    "format": "prettier --write \"**/*.{ts,tsx,json}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,json}\""
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {
    "tslib": "^2.8.1"
  }
}
